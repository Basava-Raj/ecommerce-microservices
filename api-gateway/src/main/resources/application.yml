spring:
  application:
    name: api-gateway  # Change this for each service: order-service, api-gateway
  config:
    import: optional:configserver:http://config-server:8888
  cloud:
    config:
      fail-fast: false